const styles = ({ spacing, palette }) => ({
  adminEditorWrapper: {
    padding: spacing(1),
    background: palette.white,
    marginTop: spacing(4),
    borderRadius: '4px',
  },
  editorArea: {},
});

export default styles;
